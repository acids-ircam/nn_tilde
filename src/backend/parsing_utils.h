#pragma once
#include <algorithm>
#include <cctype>
#include <iomanip>
#include <sstream>
#include <string>

bool to_bool(std::string str);
int to_int(std::string str);
float to_float(std::string str);
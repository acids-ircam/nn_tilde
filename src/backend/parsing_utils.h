#pragma once
#include <sstream>
#include <string>
#include <iomanip>
#include <algorithm>
#include <cctype>

bool to_bool(std::string str);
int to_int(std::string str);
float to_float(std::string str);